# ---------------------------------------------------------------------------
# GenTube — environment variables
# Copy to .env (not .env.local) and fill in. `pnpm db:setup` generates a local
# one with a Docker Postgres and fresh secrets.
# ---------------------------------------------------------------------------

# --- Core -------------------------------------------------------------------

# Postgres connection string. Local Docker default below; on Vercel use a
# Postgres from https://vercel.com/marketplace?category=databases
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Les tests tronquent chaque table entre chaque test : ils refusent de
# tourner ailleurs qu'en local. À poser quand DATABASE_URL désigne une base
# distante (Supabase), pour que la suite reste sur le Docker local.
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Public origin of the app, used to build absolute URLs (OAuth callbacks,
# webhook targets). No trailing slash.
BASE_URL=http://localhost:3000

# Signing key for the session JWT (cookie `session`).
#   openssl rand -hex 32
AUTH_SECRET=

# AES-256-GCM key protecting secrets at rest — today the YouTube OAuth tokens
# in `youtube_tokens`. 32 bytes, hex or base64. Rotating it makes every stored
# token undecryptable, so tenants must reconnect their channel.
#   openssl rand -hex 32
ENCRYPTION_KEY=

# --- Cloudflare R2 (asset storage) ------------------------------------------
# Every object key MUST be prefixed with `<tenant_id>/`. Assets are served
# through short-lived signed URLs — the bucket stays private.
#
# NOT WIRED UP YET. lib/storage/index.ts freezes the contract (AssetStore,
# assetKey) and throws StorageNotConfiguredError until an R2 implementation
# lands. Voice-over, image generation, video generation and the final render
# all wait on it — it is the first blocking dependency of the pipeline.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=gentube
# Préfixe racine, uniquement si le bucket est partagé avec un autre projet.
# Laisser vide quand le bucket est dédié à GenTube.
R2_PREFIX=
# Optionnel : déduit de R2_ACCOUNT_ID s'il est vide
# (https://<account_id>.r2.cloudflarestorage.com). À renseigner seulement pour
# un bucket derrière un domaine personnalisé.
R2_ENDPOINT=
# Pas de R2_PUBLIC_URL : le bucket ne doit jamais etre public. Tout se lit par
# une URL signee a duree de vie courte (AssetStore.signedUrl). Un bucket ouvert
# annulerait l'isolation multi-tenant : deviner 7/voice/42/1.mp3 suffirait a
# ecouter la narration d'un client.

# --- DeepSeek (script + storyboard generation) -------------------------------
# OpenAI-compatible API: POST /chat/completions, bearer auth, JSON mode.
# Two things were confirmed against the live account rather than assumed:
#   - the model ids are `deepseek-v4-flash` and `deepseek-v4-pro`. There is no
#     `deepseek-chat` on this account.
#   - they are REASONING models: the answer is preceded by internal reasoning
#     billed as completion tokens. A tight token budget is spent entirely on
#     reasoning and comes back HTTP 200, finish_reason "length", content EMPTY.
#     Hence the wide default below — raise it before blaming the model.

DEEPSEEK_API_KEY=
# Default: deepseek-v4-flash. The `pro` variant reasons longer and costs more.
# DEEPSEEK_MODEL=deepseek-v4-flash
# Covers reasoning AND the answer. Default: 8000.
# DEEPSEEK_MAX_TOKENS=8000
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# --- Replicate (plans animés) -----------------------------------------------
# Trois modèles, choisis par lib/video/provider.ts selon la résolution et le
# type de plan (docs/providers.md) :
#   480p                  wan-video/wan-2.2-i2v-fast   0,05 $ le clip de 5 s
#   720p                  prunaai/p-video              0,02 $ la seconde
#   avatar, lip-sync      prunaai/p-video-avatar       0,025 $ la seconde
#
# Wan facture AU CLIP : une scène de 3 s coûte le prix d'une de 5 s. D'où le
# plancher de cinq secondes imposé au storyboard.
#
# Piège de compte, sans message d'erreur : Replicate ralentit progressivement le
# débit quand le crédit s'épuise, jusqu'à une requête par seconde sans moyen de
# paiement enregistré. Rechargement automatique activé, solde au-dessus de 20 $.

REPLICATE_API_TOKEN=
# Shared secret Replicate signs its webhooks with. Get it once from
# https://api.replicate.com/v1/webhooks/default/secret — incoming webhooks are
# rejected unless the signature verifies.
REPLICATE_WEBHOOK_SECRET=

# --- Qui anime, et avec quel modele -----------------------------------------
# Le fournisseur de clips, lu par lib/video/index.ts. Vide = replicate, le seul
# dont la route de rappel existe et dont la table de prix est branchee.
# Valeurs : replicate | atlas | novita. Un nom inconnu retombe sur replicate
# plutot que de faire tomber la generation.
VIDEO_PROVIDER=

# --- Atlas Cloud (catalogue de modeles video) -------------------------------
# Pres de deux cents modeles video derriere une signature unique : c'est ce qui
# permet de comparer Wan, Seedance, Kling ou Veo en changeant une variable.
# Base : https://api.atlascloud.ai/api/v1 - doc : https://www.atlascloud.ai/docs
#
# Sa route de rappel existe : app/api/webhooks/atlas, signature Ed25519 verifiee
# contre leur JWKS (recupere seul, aucun secret a poser).
#
# Ce qui manque encore : le prix. POST /model/calculate le rend avant de
# generer, mais les noms de champs de sa reponse ne sont pas documentes. Tant
# qu'il n'est pas lu, un clip Atlas est compte a zero et sort de la marge.
ATLAS_API_KEY=
# Le modele. Vide = atlascloud/wan-2.2/image-to-video, le meme qu'en service
# chez Replicate pour que la comparaison porte sur le fournisseur. Seul le
# schema de la famille image-to-video est lu : un modele text-to-video ou
# reference-to-video attend un autre corps.
ATLAS_MODEL=

# --- ElevenLabs (voice-over) ------------------------------------------------
# The voice-over is not a garnish: it runs BEFORE the expensive visuals and
# before any payment, because the real length of the audio is what a scene
# lasts and therefore what the customer is charged. Without this key a video
# cannot be validated at all — its price would only ever be an estimate.
#
# The endpoint used is /text-to-speech/{voice}/with-timestamps, which returns
# the audio and a character-level alignment. That alignment is what gives the
# measured duration and the word-by-word karaoke subtitles.

ELEVENLABS_API_KEY=
# Fallback voice when neither the video nor the project names one. Accepts a
# short name (george, liam, antoni, anais, rachel) or a raw ElevenLabs id.
ELEVENLABS_DEFAULT_VOICE_ID=
# ELEVENLABS_MODEL=eleven_multilingual_v2
# ELEVENLABS_BASE_URL=https://api.elevenlabs.io/v1

# --- Edge TTS (la voix qui mesure) ------------------------------------------
# Aucune clé, et c'est le point : le prix d'une vidéo est la somme des durées de
# ses scènes, et une durée s'obtient en faisant lire la phrase. Il faut donc
# parler AVANT que le client valide — y compris pour les devis qui n'aboutissent
# jamais. Cette passe fixe le prix ; la voix du plan est livrée après.
#
# Ce n'est pas une API publique : c'est le service de lecture à voix haute du
# navigateur Edge. Ni contrat, ni SLA, et elle se trouve sur le chemin du prix.
# Le jour où elle casse, basculer la mesure sur Polly (lib/voice/index.ts).

# Coupe Edge TTS. La suite de tests le pose pour ne jamais sortir sur le réseau.
# EDGE_TTS_DISABLED=1
# Voix par défaut : lea. Mêmes noms courts que Polly (remi, gabrielle), pour que
# l'aperçu et la vidéo livrée aient la même voix.
# EDGE_TTS_VOICE=lea
# EDGE_TTS_TIMEOUT_MS=30000

# --- Amazon Polly (voix par défaut) -----------------------------------------
# C'est le plan qui décide qui parle : Polly Neural sur Starter — la marge de
# 41 % en dépend —, ElevenLabs sur Pro et Business, dont le français est
# nettement moins robotique (docs/tarifs.md). Polly réutilise les clés AWS du
# rendu Lambda : un seul compte, une seule paire de clés.
#
# Deux appels par scène, et ce n'est pas évitable : Polly rend soit l'audio,
# soit les speech marks, jamais les deux. Or les speech marks sont les timings
# mot à mot, donc les sous-titres karaoké. Les caractères sont facturés deux
# fois — ce qui reste un ordre de grandeur sous ElevenLabs.
#
# Polly ne dit ni la durée de l'audio ni la fin du dernier mot. La durée, qui
# est le prix, est lue dans les en-têtes de trames du mp3 (lib/voice/mp3.ts).

# Seulement si Polly doit être pris ailleurs que la région de rendu.
# POLLY_REGION=
# Voix par défaut : lea. Aussi remi, gabrielle (fr-CA). Celine et Mathieu n'ont
# pas de variante neural — un appel neural sur elles échoue.
# POLLY_DEFAULT_VOICE=lea
# POLLY_ENGINE=neural

# Force un fournisseur quel que soit le plan. Pour une machine qui n'a les clés
# que d'un seul — pas pour arbitrer en production.
# VOICE_PROVIDER=polly

# --- Cloudflare Workers AI (image shots: Flux) ------------------------------

CLOUDFLARE_AI_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# --- YouTube Data API (OAuth + publishing) ----------------------------------
# Redirect URI to register: ${BASE_URL}/api/youtube/callback
# Quota: uploads have had their own dedicated bucket since June 2026, about 100
# calls a day, and an upload has cost ~100 units instead of 1600 since December
# 2025. So roughly 100 publications a day. The limit is still project-wide, not
# per tenant, but it is no longer something to request an increase for.

YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=

# --- n8n orchestrator -------------------------------------------------------
# n8n never touches Postgres directly: it calls signed internal API routes.
# This secret authenticates Next → n8n (the webhook that starts production).
# The other direction (n8n → Next) uses INTERNAL_API_TOKEN.
#   openssl rand -hex 32
N8N_WEBHOOK_SECRET=
# Base URL of the n8n instance the app calls to start a workflow.
N8N_BASE_URL=
# Jeton d'API n8n. Il ne sert pas a l'application : c'est celui du serveur MCP
# qui laisse l'agent lire et ecrire les workflows dans n8n.
# n8n : Settings > n8n API > Create an API key.
N8N_API_TOKEN=

# --- Routes internes ---------------------------------------------------------
# Jeton porte par les routes /api/internal, en `Authorization: Bearer <jeton>`.
# C'est le nom du contrat (docs/contrats.md §2). n8n l'envoie vers Next.
# N8N_WEBHOOK_SECRET authentifie l'autre sens : Next reveille n8n.
# Tant que Cosme n'a pose qu'une des deux, le code lit INTERNAL_API_TOKEN
# puis se rabat sur N8N_WEBHOOK_SECRET.
#   openssl rand -base64 32
INTERNAL_API_TOKEN=

# --- SasPay (encaissement mobile money, XOF) ---------------------------------
# Remplace GeniusPay depuis le 5 septembre 2026. Encaissement MTN, Moov et
# Celtiis Cash au Benin. Base : https://api.saspay.me/api/v1
#
# Le prestataire est choisi par PAYMENT_PROVIDER (vide = saspay, le seul
# branche). Les cles bac a sable et production coexistent : SASPAY_ENV choisit,
# et le prefixe de la cle est verifie contre lui au demarrage — une sk_live_
# posee sous SANDBOX encaisserait de l'argent reel en croyant tester.
PAYMENT_PROVIDER=
SASPAY_ENV=sandbox

# Cles a generer dans l'espace Developpeur de https://app.saspay.me
SASPAY_SANDBOX_API_KEY=
SASPAY_LIVE_API_KEY=

# Secret de signature du webhook, a poser dans app.saspay.me > Webhooks.
# IMPORTANT : saisis TA propre valeur dans leur formulaire plutot que de la
# laisser vide. Une valeur generee n'est affichee qu'une seule fois et n'est
# plus jamais consultable — la perdre oblige a recreer le webhook.
SASPAY_SANDBOX_WEBHOOK_SECRET=
SASPAY_LIVE_WEBHOOK_SECRET=

# L'URL a declarer chez eux : https://<votre-domaine>/api/webhooks/saspay
# Leur rappel ne porte aucun identifiant venant de nous : il sert de reveil, et
# c'est lib/billing/reveil.ts qui relit nos encaissements en attente.
